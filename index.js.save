// Importa Express
import express from "express";
import cors from "cors";

const app = express();
const PORT = 3000;

// Middleware
app.use(cors());
app.use(express.json());

// Route di test
app.get("/", (req, res) => {
  res.send("✅ Server Express attivo su porta 3000!");
});

// Avvio del server
app.listen(PORT, () => {
  console.log(`🚀 Server in ascolto su http://localhost:${PORT}`);
})
v
